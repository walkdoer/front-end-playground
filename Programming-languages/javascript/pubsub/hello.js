define('andrew', ['jquery'], function (name, ui) {
    'use strict';
    console.log(ui);
    for (var i=0; i < name.length; i++) {
        console.warn(name[i]);
    }
    function tryToLearnVimFrom(ta, ni) {
       
    }
});
